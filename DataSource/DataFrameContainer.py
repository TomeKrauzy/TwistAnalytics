class DataFrameContainer:

    def __init__(self, production, sales, costs, production_resource, stores, products, products_yield):
        self.production = production
        self.sales = sales
        self.costs = costs
        self.production_resource = production_resource
        self.stores = stores
        self.products = products
        self.products_yield = products_yield
